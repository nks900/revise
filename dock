pending:
CACHING and layers
changing image to latest version inside a dockerfile
tagging image
tag override
run containers with different tags /// versioning images and containers
pushing images n tags

kodekloud docker lab, docker storage, entry point, networking ..
-------------------------------------------------------------
ENTRYPOINT override, 

==temporary hold====
